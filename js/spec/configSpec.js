define(["spec/SpecHelper", "config"], function(SpecHelper, config) {

    describe("config", function() {

        beforeEach(function() {
            // spyOn(window, "location")
        });

        it("should work", function() {
            expect(true).toEqual(true);
        });

    });

});

